{
  "name": "tests-security",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:failures": "SHOW_FAILURES=true vitest run"
  },
  "devDependencies": {
    "supertest": "^6.3.3",
    "@types/supertest": "^2.0.16",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
