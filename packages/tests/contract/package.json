{
  "name": "tests-contract",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run src/consumer.test.ts",
    "test:failures": "SHOW_FAILURES=true vitest run src/consumer.test.ts",
    "test:provider": "vitest run src/provider.test.ts"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^11.0.2",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
